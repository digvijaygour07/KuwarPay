{
  "name": "kuwarpay",
  "version": "1.0.0",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild netlify/functions/api/api.js --bundle --platform=node --outdir=.netlify/functions --allow-overwrite",
    "dev": "nodemon netlify/functions/api/api.js",
    "build:lambda": "esbuild api.js --bundle --platform=node --outdir=./ --allow-overwrite"
  },
  "author": "Your Name or Organization",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "crypto-js": "^4.2.0",
    "dotenv": "^16.4.5",
    "email-validator": "^2.0.4",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.14",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "nodemon": "^3.1.4"
  }
}
